package interfaz;
import dominio.*;
import java.util.ArrayList;

public class Interfaz{
	
	public String procesarPeticion(){
		ArrayList<String> lista = new ArrayList<>();
		LenguajeAB generarAB = new LenguajeAB();

		for(int i = 0; i < 20; i++){
		//	Stream.generate(() -> generarAB.gramatica(i/2, "") + generar
		}

		return "";
	}
}
